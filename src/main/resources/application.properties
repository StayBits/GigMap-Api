spring.application.name=GigMap-api

# --- DataSource (MySQL Local) ---
spring.datasource.url=jdbc:postgresql://db.sekqmrmzrnwzjbhmyipx.supabase.co:5432/postgres?user=postgres&password=DaDeval202010
spring.datasource.username=postgres
spring.datasource.password=DaDeval202010
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true
# No forces el dialecto: Hibernate 6 lo infiere solo
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=com.staybits.gigmapapi.shared.infrastructure.persistence.jpa.configuration.strategy.SnakeCaseWithPluralizedTablePhysicalNamingStrategy

# --- Hikari (arranque mas tolerante) ---
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.initializationFailTimeout=0

# --- Docs (del pom) ---
documentation.application.description=@project.description@
documentation.application.version=@project.version@

# --- Security Configuration ---
# JWT Authentication enabled - endpoints protected except /api/v1/auth/**

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
jwt.secret=mySecretKeyForJWTTokenGeneration12345changethisinsecureproductionkey

# JWT Token Expiration (in milliseconds) - Default: 24 hours
jwt.expiration=86400000
